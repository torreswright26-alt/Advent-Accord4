# Advent Accord Render LoRA Dataset

This folder contains explicitly approved render references and a machine-readable manifest.

- `approved/`: approved images renamed in chronological approval order
- `approved_manifest.json`: metadata and training-use guidance
- Primary benchmark: approval `002`

The manifest should be updated whenever a new image is explicitly approved.
